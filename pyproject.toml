[project]
name = "hw05"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Dan Greco", email = "me@dangre.co" }
]
requires-python = ">=3.13"
dependencies = [
    "bokeh>=3.8.0",
    "duckdb>=1.4.0",
    "numpy>=2.3.3",
    "pandas>=2.3.3",
]

[project.scripts]
hw05 = "hw05:main"

[build-system]
requires = ["uv_build>=0.8.14,<0.9.0"]
build-backend = "uv_build"

[tool.ty.environment]
python = "./.venv"
